/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig


module.exports = {
    images: {
      domains: ['daisyui.com'], // Add the domain(s) from which you'll load images
    },
  };